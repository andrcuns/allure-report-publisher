component_management:
  individual_components:
    - component_id: commands
      name: commands
      paths:
        - lib/allure_report_publisher/commands/**
    - component_id: uploaders
      name: uploaders
      paths:
        - lib/allure_report_publisher/lib/uploaders/**
    - component_id: providers
      name: providers
      paths:
        - lib/allure_report_publisher/lib/providers/**
    - component_id: helpers
      name: helpers
      paths:
        - lib/allure_report_publisher/lib/helpers/**
